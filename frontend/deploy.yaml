apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app1-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: nerdinyou/prod_fe:1
          ports:
            - containerPort: 4000
          env:
            - name: BACKEND_URL
              value: http://backend-svc:5000/api/message
          resources:
            requests:
                cpu: "250m"
                memory: "400Mi"
            limits:
              cpu: "500m"
              memory: "450Mi"
